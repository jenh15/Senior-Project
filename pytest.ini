[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    integration: marks tests that hit live external APIs (deselect with -m "not integration")
    slow: marks tests that are slow to run
